Algoritmo "calc_trapezio"

Var
//não é permitido declarar duas variaveis iguais
   b1, b2, h, area : real
Inicio

   b1 <- 6
   b2 <- 8
   h  <- 5

   area <- (b1 + b2) / 2 * h

   escreval(area)


Fimalgoritmo